/*
    setDirection(); //sets direction to -1 or 1;
      speed.add(acceleration);
      textSize(32);
      text("elevator y: " + String.valueOf(loc.y), 10, 50);
      //text("speedlimit: " + speed.limit(maxSpeed * direction), 10, 100);

      //PVector acc = acceleration.mult(direction);
      //PVector acc = acceleration;
      //text("acc: " + acc.y, 10, 200);
      text("speed: " + speed.y, 10, 220);
      //speed.add(acc); //---------------------
      text("vel2: " + speed.y, 10, 240);


      loc.add(speed.mult(direction)); //---------------------
      //acceleration.mult(0); //---------------

*/
